{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "corepack enable && corepack prepare pnpm@9.15.0 --activate && pnpm -w -F @ai-vibe/web build",
  "devCommand": "corepack enable && corepack prepare pnpm@9.15.0 --activate && pnpm -C apps/web dev",
  "installCommand": "corepack enable && corepack prepare pnpm@9.15.0 --activate && pnpm install",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next",
  "env": {
    "NODE_ENV": "production"
  }
}
